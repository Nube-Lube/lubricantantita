import SwiftUI

@main
struct WirelessMusicApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .background(Color(hex: "080809").ignoresSafeArea())
        }
    }
}
